# Architecture

GenAI Mentor receives a user query, checks safety, routes the query, retrieves sources if needed, calls tools when useful, generates an educational answer, checks grounding/citations, and returns the final answer.

Flow:
